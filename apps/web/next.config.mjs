/** @type {import('next').NextConfig} */
// BUGFIX: Generated Next.js build artifacts are excluded from repository tooling.
const nextConfig = {
  poweredByHeader: false,
  reactStrictMode: true
};

export default nextConfig;
